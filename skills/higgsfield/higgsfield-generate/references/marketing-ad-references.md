# Ad-reference video

Use an authorized reference clip or an accepted previous job as inspiration. Higgsfield's own creative-adaptation guide (published CLI skill material at https://github.com/higgsfield-ai/cli) owns analysis and the new creative direction; this file handles the provider entity.

Inspect help for the selected `marketing-studio ad-references` operation and establish its accepted source types. The original guide described an upload handle via `video-input` or a previous job via `job`; these are dated examples, not universal flags. Do not force a user into uploading again when an existing accepted source is available.

Upload a needed local clip once, create the ad-reference entity once, and record the resulting safe ID. Reconcile a lost response before another create. Poll only with a fixed observation cap/deadline and recognized states. An unknown status is unresolved, not a reason for an endless shell loop.

When ready, bind the actual ad-reference ID through the selected generation schema and carry its intended avatar/product relationships forward. Do not drop the reference between setup and generation. Do not combine it with incompatible hook/setting inputs. Inspect whether the output follows the intended reference structure while preserving the new product and brief.
