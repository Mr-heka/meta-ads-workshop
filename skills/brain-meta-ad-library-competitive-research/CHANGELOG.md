# Changelog

## 2026-07-28 — accuracy, currency + publishing pass
- **NEW: the actual Ad Library impression buckets named.** The pack told readers to sort by impressions without saying what appears. Now documents the buckets (`under 1K` / `1K-5K` / `5K-10K` / `10K-50K` / `50K-100K` / `100K-500K` / `500K-1M` / `1M+`), the **"Low Impression Count" badge on sub-100-impression ads**, how to read each band, the fact that buckets are lifetime-cumulative (which is why longevity and impressions must be read together), and the extra EU transparency data.
- **Dynamic-creative "multiple versions" tell flagged as unreliable.** Dynamic Creative was retired into Flexible / Advantage+ creative, so the label no longer distinguishes a deliberate permutation test from Meta's own automation. Flagged at the Ads And Leads method, consensus point 7, the IF/THEN rule and the defaults list; the "dynamic ad at 6+ months = super winner" default is retired outright. Duplicate count named as the replacement signal that still works.
- **Read-window standardised.** Parker's 48-72 hours / 3,000-5,000 impressions is now labelled a directional read under the single house verdict gate ($100 + 7 days + 3 conversions), in the defaults list and the IF/THEN rule.
- **DCT container correction (dated)** added where the pack prescribed a "3-2-2 DCT test ad set", with the two current options spelled out (separate standard ads vs Flexible media with enhancements off).
- **Publishing scrub.** Ad account and pixel IDs replaced with placeholders in SKILL.md and the worked example; offer ladder, prices and CPL targets removed; "Applied to our business" renamed to "Applied to your business" and rewritten as reader-fill prompts; the example's stale "LP-only lead gen" line updated to form-first.

## 2026-07-18
- Recalibration pass: advisor CPL targets, Neiman verdict gate, workshop-paused routing note (audit 2026-07-18).

## 2026-07-06
- Brain built from 16 YouTube sources via topic-brain-builder. Engine: yt-dlp. Mix: {"new": 17, "evergreen": 7, "hidden_gems": 9, "channels": 24}.
- 2026-07-06: added `examples/workshop-competitor-mining-session.md` (worked Q&A session, workshop city campaign), linked from SKILL.md Deeper references.
